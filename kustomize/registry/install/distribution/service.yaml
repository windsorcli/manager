apiVersion: v1
kind: Service
metadata:
  name: distribution
  namespace: registry
spec:
  selector:
    app.kubernetes.io/name: distribution
  ports:
    - name: http
      port: 5000
      targetPort: http
