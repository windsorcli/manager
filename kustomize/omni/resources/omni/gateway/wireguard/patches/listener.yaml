- op: add
  path: /spec/listeners/-
  value:
    name: wireguard-udp
    protocol: UDP
    port: ${omni_wireguard_node_port}
    allowedRoutes:
      namespaces:
        from: All
      kinds:
        - kind: UDPRoute
