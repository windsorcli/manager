apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: omni-api
  namespace: omni
spec:
  secretName: omni-api-tls
  issuerRef:
    name: ${omni_cert_issuer}
    kind: ClusterIssuer
  dnsNames:
    - ${omni_hostname}
