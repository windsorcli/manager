apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: omni-api
  namespace: omni
spec:
  secretName: omni-api-tls
  issuerRef:
    name: public-acme
    kind: ClusterIssuer
  dnsNames:
    - ${omni_hostname}
